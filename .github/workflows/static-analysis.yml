name: static analysis

on: [ push ]

jobs:
 static-analysis:
  runs-on: ubuntu
  steps:
   - uses: actions/checkout@v2

   - uses: actions/setup-node@v2-beta
     with:
      node-version: '14'

   - run: yarn install

   - run: yarn run eslint
